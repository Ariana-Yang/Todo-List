### 待办清单页面调整计划

**修改目标**：调整页面配色和按钮样式，保持其他部分不变

**修改内容**：

1. **配色调整**
   - 将页面背景色从 `#f5f7fa` 修改为更浅的灰色 `#f8fafc`
   - 将主色调从 `#4A90E2` 修改为明亮的蓝色 `#3b82f6`

2. **按钮样式调整**
   - 确保"添加"按钮为圆角（已设置为8px，保持不变）
   - 将"添加"按钮背景色修改为 `#3b82f6`，文字为白色
   - 调整"添加"按钮悬停颜色为更深的蓝色
   - 将"完成"按钮背景色从 `#52C41A` 修改为 `#10b981`
   - 调整"完成"按钮悬停颜色为更深的绿色
   - 将"删除"按钮背景色从 `#FF4D4F` 修改为 `#ef4444`
   - 调整"删除"按钮悬停颜色为更深的红色

**修改文件**：
- `d:\Trae CN\Todo-List\index.html` （仅修改CSS样式部分）

**修改步骤**：
1. 修改 `body` 标签的 `background-color` 属性
2. 修改 `h1` 标签的 `color` 属性和 `task-input` 聚焦时的边框颜色
3. 修改 `add-btn` 的 `background-color` 和悬停颜色
4. 修改 `complete-btn` 的 `background-color` 和悬停颜色
5. 修改 `delete-btn` 的 `background-color` 和悬停颜色

**预期效果**：页面配色更清新明亮，按钮颜色区分更明显，悬停效果更自然